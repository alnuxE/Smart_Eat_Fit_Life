import '../models/metric_model.dart';

class DetailController {
  final Metric metric;

  DetailController(this.metric);

  // Aquí puedes agregar la lógica de negocio futura para la vista de detalles.
  // Por ejemplo: consumo de APIs de historial detallado, formateo de fechas,
  // manipulación de datos de la gráfica, etc.
}
